import { FlitertablePipe } from './flitertable.pipe';

describe('FlitertablePipe', () => {
  it('create an instance', () => {
    const pipe = new FlitertablePipe();
    expect(pipe).toBeTruthy();
  });
});
